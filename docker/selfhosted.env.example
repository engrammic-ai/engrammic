# Engrammic Self-Hosted Configuration

# Required: Your license key
ENGRAMMIC_LICENSE_KEY=ENGR_your_license_key_here

# Database passwords (defaults work for local dev, change in production)
POSTGRES_PASSWORD=engrammic

# =============================================================================
# REQUIRED: Embedding provider (pick one)
# =============================================================================
# Without embeddings, remember/learn/recall will fail.
# Uses LiteLLM - see https://docs.litellm.ai/docs/embedding/supported_embedding

# Option 1: OpenAI (recommended)
EMBEDDING_MODEL=openai/text-embedding-3-small
OPENAI_API_KEY=sk-...

# Option 2: Azure OpenAI
# EMBEDDING_MODEL=azure/text-embedding-ada-002
# AZURE_API_KEY=...
# AZURE_API_BASE=https://your-resource.openai.azure.com

# Option 3: AWS Bedrock
# EMBEDDING_MODEL=bedrock/amazon.titan-embed-text-v2:0
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_REGION_NAME=us-east-1

# Option 4: Google Vertex AI (requires gcloud credentials mount)
# EMBEDDING_MODEL=vertex_ai/text-embedding-005
# VERTEX_PROJECT=your-project
# VERTEX_LOCATION=us-central1

# Option 5: Local via Ollama
# EMBEDDING_MODEL=ollama/nomic-embed-text
# OLLAMA_API_BASE=http://host.docker.internal:11434

# EMBEDDING_DIMENSIONS must match the model (1024 for bge-m3, 768 for nomic/text-embedding-005,
# 384 for all-MiniLM-L6-v2). Default is 1024. Changing it after data exists requires
# recreating Qdrant collections and re-embedding.
# EMBEDDING_DIMENSIONS=1024

# =============================================================================
# OPTIONAL: LLM for SAGE synthesis
# =============================================================================
# The SAGE provider and model are chosen in config/models.yaml, not here. The
# default tier uses Google Vertex (Gemini). To use a different provider, override
# models.yaml via the host config directory (~/.engrammic/config/models.yaml) and
# set the matching credential below. Without usable LLM credentials, SAGE runs in
# passive mode (storage + recall only, no synthesis). See the Config Files docs.
#
# Credential for whichever provider models.yaml selects:
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=...
# VERTEX_PROJECT=your-project           # vertex (default), with VERTEX_LOCATION
# VERTEX_LOCATION=us-central1
# OLLAMA_BASE_URL=http://host.docker.internal:11434   # ollama provider
#
# Note: the LLM path uses OLLAMA_BASE_URL, the embedding path uses OLLAMA_API_BASE.
# They are different variables.

# Anonymous usage statistics (set to false to opt out)
TELEMETRY__ENABLED=true
