# Test environment overrides — used with: uv run pytest --env-file .env.test
# Start test infra first: docker compose --profile test up -d memgraph-test

ENVIRONMENT=development
MEMGRAPH_URI=bolt://localhost:17687
MEMGRAPH_USER=
MEMGRAPH_PASSWORD=

# Test Qdrant collection (isolated from prod)
QDRANT_URL=http://localhost:6333
