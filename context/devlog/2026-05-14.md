# Devlog 2026-05-14

## GCP Deployment Setup

### Infrastructure Deployed (Pulumi)
- Region: europe-north1-a (moved from us-central1)
- GCE instance: e2-standard-2 spot (~$20/mo)
- Disks: 20GB memgraph, 20GB qdrant, 10GB postgres (formatted + mounted)
- VPC, subnets, NAT, firewall rules
- Secret Manager (empty secrets created)
- GCS backup bucket

### Container Registry (Artifact Registry)
- Repo: `europe-north1-docker.pkg.dev/engrammic/engrammic/`
- Images pushed via Cloud Build:
  - `engrammic-api:latest` (188MB)
  - `engrammic-dagster:latest` (188MB)

### IAM Service Accounts
| SA | Purpose | Roles |
|----|---------|-------|
| `cloudbuild-dev@` | Cloud Build | storage.objectAdmin, artifactregistry.writer, logging.logWriter |
| `stateful-host-dev@` | GCE instance | secretmanager.secretAccessor, logging.logWriter, monitoring.metricWriter, artifactregistry.reader, storage.objectAdmin |
| `context-service-run-dev@` | Cloud Run (future) | secretmanager.secretAccessor, cloudtrace.agent, monitoring.metricWriter |

### Files Added/Changed
- `infra/just.infra` - justfile recipes for SSH, tunnels, diagnostics, Cloud Build
- `infra/docker-compose.infra.yml` - infra services for GCE (memgraph/qdrant/redis/postgres)
- `cloudbuild.api.yaml` - Cloud Build config for API image
- `cloudbuild.dagster.yaml` - Cloud Build config for Dagster image
- `.gcloudignore` (monorepo root) - excludes .venv/.git etc (1.8GB -> 11.6MB upload)
- `infra/components/iam.py` - added Cloud Build SA, AR reader for GCE

### Codebase Cleanup (earlier)
- Fixed 5 remaining Memgraph EXISTS query incompatibilities
- Removed unused Jina/VertexAI embedding aliases
- Removed GET_NODE legacy alias
- Removed stub summarization_retry sensor
- Fixed logging.py type signature

### Deployed to GCE
- Infra services: memgraph, qdrant, redis, postgres all healthy
- API container pulled from AR, running on port 8000
- Switched embeddings from Jina to Vertex AI (`text-embedding-005`)
- Fixed memgraph startup issues (removed broken healthcheck, using docker volume)

### Fixes During Deploy
- Memgraph crash: `lost+found` in mount dirs, invalid healthcheck, log permission denied
- Cloud Build SA needed `roles/logging.logWriter` for CLOUD_LOGGING_ONLY
- GCE disks needed subdirs to avoid `lost+found` conflicts

### Next
- Test MCP tools via tunnel
- Deploy Dagster containers
- Set up proper secrets in Secret Manager
