# v1.2b Protocol Migration

**Status:** Ready
**Branch:** v1.2b-protocol-migration
**Created:** 2026-05-03
**Updated:** 2026-05-03

Migrate 7 files to use HyperGraphStore protocol instead of direct Memgraph access.

## Pre-reqs (completed 2026-05-03)

- [x] Fixed silo_id type inconsistency (UUID -> str) in protocol and memgraph_store
- [x] Added `Closeable` protocol for lifecycle management
- [x] Removed app.py from migration list (composition root, needs concrete types)

## Background

CLAUDE.md rule 8: "Don't bypass `engine/protocols.py` - depend on the interface, not on `memgraph_store` / `qdrant_store` directly."

Currently tracked in `tests/test_architecture_boundary.py`.

## Files to Migrate

7 files (app.py excluded - it's the composition root that constructs concrete instances):

1. `src/context_service/signals/cursor.py`
2. `src/context_service/signals/heat.py`
3. `src/context_service/clustering/service.py`
4. `src/context_service/extraction/service.py`
5. `src/context_service/services/evidence.py`
6. `src/context_service/services/silo.py`
7. `src/context_service/api/deps.py`

**Not migrating:**
- `api/app.py` - composition root, needs concrete MemgraphClient for lifecycle (close(), bootstrap schema)

## Migration Pattern

Each file should:
1. Import from `engine.protocols` instead of `stores.memgraph` / `engine.memgraph_store`
2. Accept `HyperGraphStore` protocol in function signatures
3. Use protocol methods only (no Memgraph-specific calls)

Note: `execute_query`/`execute_write` are escape hatches on the protocol. Using them is allowed but counts as tech debt (still coupled to Cypher dialect).

## Tasks

### Phase 1: Migrate leaf files

No dependents, safe to change:
- signals/cursor.py
- signals/heat.py

### Phase 2: Migrate service layer

- services/evidence.py
- clustering/service.py
- extraction/service.py (coordinate with Dagster asset)

### Phase 3: Migrate silo and deps

- services/silo.py
- api/deps.py (vends HyperGraphStore to route handlers)

### Phase 4: Update boundary test

Verify `tests/test_architecture_boundary.py` passes with no violations.

### Phase 5: Clean up union types

Audit `db/custodian_queries.py` for `MemgraphClient | HyperGraphStore` unions and eliminate them.

## Done Criteria

- [ ] All 7 files use protocol imports
- [ ] No direct memgraph_store imports outside engine/
- [ ] Boundary test passes
- [ ] `just check` passes
- [ ] `just test` passes
- [ ] No union types in db/ layer
