# v1.2b Protocol Migration

**Status:** Draft
**Branch:** v1.2b-protocol-migration
**Created:** 2026-05-03

Migrate 8 files to use HyperGraphStore protocol instead of direct Memgraph access.

## Background

CLAUDE.md rule 8: "Don't bypass `engine/protocols.py` - depend on the interface, not on `memgraph_store` / `qdrant_store` directly."

Currently tracked in `tests/test_architecture_boundary.py`.

## Files to Migrate

From v1.2-backlog.md:

1. `src/context_service/signals/cursor.py`
2. `src/context_service/signals/heat.py`
3. `src/context_service/clustering/service.py`
4. `src/context_service/extraction/service.py`
5. `src/context_service/services/evidence.py`
6. `src/context_service/services/silo.py`
7. `src/context_service/api/deps.py`
8. `src/context_service/api/app.py`

## Migration Pattern

Each file should:
1. Import from `engine.protocols` instead of `stores.memgraph` / `engine.memgraph_store`
2. Accept `HyperGraphStore` protocol in function signatures
3. Use protocol methods only (no Memgraph-specific calls)

## Tasks

### Phase 1: Audit current violations

For each file, document:
- What Memgraph-specific imports exist
- What methods are called that aren't on protocol
- Whether new protocol methods are needed

### Phase 2: Extend protocol if needed

If any file uses Memgraph methods not on protocol:
- Add to `engine/protocols.py` (if generally useful)
- Or refactor the callsite

### Phase 3: Migrate files (dependency order)

Start with leaf files (no dependents), work up:
1. signals/cursor.py, signals/heat.py
2. services/evidence.py
3. clustering/service.py, extraction/service.py
4. services/silo.py
5. api/deps.py, api/app.py

### Phase 4: Update boundary test

Verify `tests/test_architecture_boundary.py` passes with no violations.

## Done Criteria

- [ ] All 8 files use protocol imports
- [ ] No direct memgraph_store imports outside engine/
- [ ] Boundary test passes
- [ ] `just check` passes
- [ ] `just test` passes
