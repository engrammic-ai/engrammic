# v1.3e Causal Completion

**Status:** Not started
**Parent:** [v1.3-master.md](./v1.3-master.md)
**Depends on:** [v1.3-phase-0-flags.md](./v1.3-phase-0-flags.md)
**Created:** 2026-05-03
**Est:** 2-3 days

Complete causal edge handling: transitive invalidation, CORROBORATES semantics, and partial revision.

## Existing Infrastructure

| Component | Location | Status |
|-----------|----------|--------|
| `inferred_from_edge_ids` | `engine/models.py` (line 93) | Exists |
| Transitivity asset | `pipelines/assets/causal.py` | Exists (full silo sweep) |
| CORROBORATES direction | `db/queries.py` | Already directed `(a)-[:CORROBORATES]->(b)` |
| `revise_belief` | `engine/revision.py` | Exists |
| CausalConfig flags | `config/settings.py` | Exists |

## Scope

| Item | Description |
|------|-------------|
| Transitive invalidation | When source edge superseded, tombstone derived inferred edges |
| CORROBORATES semantics | Document directed semantics (A supports B, not symmetric) |
| Partial revision | Split beliefs when only part is revised |

## Design Decisions (locked)

| Question | Decision | Rationale |
|----------|----------|-----------|
| CORROBORATES directionality | Directed (A supports B) | Already implemented this way in db/queries.py |
| Partial revision method | LLM-based split | Heuristic too brittle for semantic splitting |
| Cascade depth limit | 3 hops max | Config setting `max_invalidation_depth: 3` |

## Tasks

### Transitive Invalidation

1. **Verify `inferred_from_edge_ids` tracking** - confirm it's populated by transitivity asset
2. **Implement invalidation trigger** - on edge supersession, find derived edges via `inferred_from_edge_ids`
3. **Tombstone derived edges** or mark `invalidated: true`
4. **Add `max_invalidation_depth` config** (default 3)
5. **Handle partial re-run** - transitivity asset currently does full sweep; add subgraph mode or accept full re-run

### CORROBORATES Semantics

1. **Document in specs** - A CORROBORATES B means "A provides supporting evidence for B"
2. **Verify extraction prompt** aligns with directed semantics
3. **No code change needed** - already directed in Cypher

### Partial Revision

1. **Extend `revise_belief`** in `engine/revision.py` to support splitting
2. **Add LLM prompt** for belief splitting (identify which claims within belief are revised)
3. **Create child beliefs** linked to original via REVISED_FROM
4. **Tests**

## Done Criteria

- [ ] Transitive invalidation works on supersession
- [ ] CORROBORATES semantics documented
- [ ] Partial revision splits beliefs correctly
- [ ] Cascade depth configurable
- [ ] Tests pass
- [ ] `just check` passes
