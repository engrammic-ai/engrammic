# v1.2 Adversarial Review Synthesis

**Date:** 2026-05-03
**Reviewers:** Architecture, Testing, Operations critics

---

## v1.2b Protocol Migration

### Critical Issues

| Issue | Source | Fix |
|-------|--------|-----|
| **app.py shouldn't be migrated** - it's composition root, not protocol consumer | Arch | Remove from list; keep concrete for lifecycle management |
| **close() not on protocol** - app.py needs it, will break mypy strict | Arch | Add `Closeable` protocol or keep app.py concrete |
| **silo_id type inconsistency** - some methods take UUID, some take str | Arch | Fix protocol to use str consistently before migration |
| **db/indexes.py blocking** - schema management typed to MemgraphClient | Arch | Keep DDL at concrete level, don't leak into protocol |

### Test Gaps

| Gap | Fix |
|-----|-----|
| Boundary test only checks imports, not runtime behavior | Add `test_protocol_structural_conformance.py` |
| No FakeGraphStore enforcement in migrated module tests | Lint rule or fixture enforcement banning AsyncMock |
| 5 modules have no unit tests before migration | Add tests for evidence.py, silo.py before migrating |
| No partial-migration test | Add intermediate state test coverage |

### Ops Concerns

| Concern | Fix |
|---------|-----|
| No staging deploy requirement | Add "deploy to staging + smoke test MCP tools" step |
| Dagster asset coordination | extraction/service.py migration must update Dagster asset |
| Union types in custodian_queries.py | Clean up `MemgraphClient | HyperGraphStore` unions |

### Revised Migration List

Remove `api/app.py`. Final list (7 files):
1. signals/cursor.py
2. signals/heat.py
3. services/evidence.py
4. clustering/service.py
5. extraction/service.py
6. services/silo.py
7. api/deps.py

---

## v1.2c Causal Edges

### Blockers (must resolve before Phase 1)

**The plan is a stub, not a plan.** All three reviewers flagged this.

| Decision | Options | Recommendation |
|----------|---------|----------------|
| **Where does inference run?** | Custodian batch / Query-time / Dagster asset | Dagster asset (batch) - safest for latency |
| **Materialized vs virtual?** | Store inferred edges / Compute at query | Materialized with invalidation - query-time blows 250ms target |
| **Confidence formula?** | Multiplicative / Weighted / Source-aware | Multiplicative with source-deduplication |
| **PREVENTS exists?** | No - needs primitives change | Add to CITEEdgeType in primitives first |

### Architecture Issues - Partially Resolved 2026-05-03

| Issue | Fix | Status |
|-------|-----|--------|
| PREVENTS missing from CITEEdgeType | Primitives PR required before Phase 1 | Done (in primitives) |
| Extraction confidence != edge confidence | Model as separate fields: `extraction_confidence` vs `consensus_confidence` | Done |
| CORROBORATES directionality unclear | Define semantics: A supports B (directed) vs mutual (symmetric) | Open |
| Conflicts with patterns.py `causal_chain` stub | Coordinate or consolidate | Open |
| Transitive invalidation strategy missing | Tombstone derived edges when source edges superseded | Open |
| Silo isolation on multi-hop queries | Filter every hop by silo_id | Done |
| Source-dedup by value not edge ID | Dedup by edge ID in _compute_confidence | Done |
| asyncio.run pattern in causal asset | Added _run_async_int helper for nested loops | Done |
| Node.silo_id vs BinaryEdge.silo_id mismatch | BinaryEdge.silo_id now uuid.UUID | Done |
| NOT EXISTS may create duplicates | MERGE key excludes id, only set on CREATE | Done |

### Test Gaps (Critical) - RESOLVED 2026-05-03

All gaps addressed in `tests/test_causal_transitivity.py`:

| Missing Test | Purpose | Status |
|--------------|---------|--------|
| `test_causal_transitivity_basic` | A->B->C => inferred A->C | Done |
| `test_causal_transitivity_depth_cap` | Max 3 hops enforced | Done |
| `test_causal_cycle_terminates` | A->B->A doesn't explode | Done |
| `test_causal_inferred_flag_set` | inferred=true on derived edges | Done |
| `test_prevents_enum_exists` | PREVENTS in CITEEdgeType | Done |
| `test_corroboration_aggregation` | Multi-source boosts confidence | Done |
| `test_causal_silo_isolation` | Edges don't cross silos | Done |

### Ops Requirements (Non-negotiable)

| Requirement | Why |
|-------------|-----|
| Feature flag `causal_extraction_enabled` | Rollback without deleting data |
| Dagster asset for tombstoning bad inferred edges | Undo wrong inference runs |
| Confidence histogram metric | Detect LLM hallucination early |
| Audit logging via extraction/filter/audit.py | Trace causal claims |
| Re-extraction decision for existing silos | Or mark `coverage_epoch` on edges |

### Settings.py Structural Issue

Both plans will worsen the existing problem: flat shims duplicate nested config objects (e.g., `walker_alpha` exists in both `RetrievalTuning.walker_alpha` and `WalkerTuning.alpha`). 

**Before adding causal settings:** Either remove flat shims or add model_validator sync. New settings should only go in nested config.

---

## Recommended Actions

### v1.2b - Ready after fixes
1. Remove app.py from migration list
2. Fix silo_id type inconsistency in protocol (use str)
3. Add unit tests for evidence.py, silo.py
4. Document escape-hatch Cypher usage as debt
5. Then proceed with migration

### v1.2c - Blocked on design
1. Answer the 4 design questions (create v1.2c-design-decisions.md)
2. Add PREVENTS to CITEEdgeType in primitives
3. Add feature flag infrastructure
4. Only then write implementation plan
