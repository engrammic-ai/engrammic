# 2026-05-06

## LiteLLM Migration

Replaced 4 custom LLM provider clients (Anthropic, OpenAI, Gemini, Vertex) with unified LiteLLM wrapper. -749 lines. Now supports Ollama/vLLM for self-hosted with zero additional code.

Also added LiteLLM as an embedding provider option alongside Jina/Vertex.

## Architecture Decision

Established separation: pydantic-ai for agents (custodian reasoning, tool use), litellm for one-shot calls (extraction, classification, summarization). Stored in Engrammic memory.

## Bug Fixes

- Wired `max_tokens` param that was silently ignored in all non-Anthropic LLM clients
- Added truncation detection logging when output gets cut off
- Wired `as_of` temporal filtering in `Context.query()`
- Fixed version mismatch (`__init__.py` said 1.3.0, now 1.7.0)

## Config

- Enabled heat/decay/write-events in `.env`
- Updated `.env.example` with litellm settings
- Documented heat formula in Engrammic memory

## Commits

1. `fix: wire ignored parameters and add truncation detection`
2. `refactor(llm): migrate to litellm for unified provider support`
3. `fix(llm): wire schema in extract_structured, fix pipeline import`
4. `feat(embeddings): add LiteLLM embedding service as unified provider option`
5. `docs: update .env.example with litellm and embedding settings`
6. `fix: sync __init__.py version to 1.7.0`

## Next

- Heat scores now accumulating - verify with queries after some usage
- Consider removing old Jina/Vertex embedding files once litellm embeddings proven stable
